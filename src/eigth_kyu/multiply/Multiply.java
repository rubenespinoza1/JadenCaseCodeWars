package eigth_kyu.multiply;

//Para este problema solo se debia poner el punto y coma faltante en return a * b
public class Multiply {
    public static Double multiply(Double a, Double b) {
        return a * b; // a * b -> a * b;
    }
}